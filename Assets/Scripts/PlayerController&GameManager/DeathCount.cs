using UnityEngine;
using UnityEngine.UI;

public class DeathCount : MonoBehaviour
{
    //public Text deathCounterText;

    void Update()
    {
        //deathCounterText.text = "Deaths: " + SavePointManager.GetDeathCount().ToString();
        //Debug.Log("DeathCount: " + SavePointManager.GetDeathCount().ToString());
    }
}
